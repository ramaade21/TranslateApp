<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only network security config. Allows cleartext HTTP traffic to the
  Android emulator's host-loopback alias (10.0.2.2) so the app can reach a
  locally-run backend during development. This file lives under src/debug
  so it is NEVER packaged into a release build - release builds fall back
  to the platform default, which requires HTTPS everywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
